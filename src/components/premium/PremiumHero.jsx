import React from 'react';
import { Header } from '../common';

const PremiumHero = () => {
  return (
    <div className="relative min-h-screen">
      {/* Navigation Bar with premium variant */}
      <Header variant="premium" />
    </div>
  );
};

export default PremiumHero;